@model Services.ViewModels.AccountsViewModel

<tr>
    <td>@Model.Id</td>
    <td>@Model.Frequency</td>
    <td>@Model.Created.ToString("yyyy-MM-dd")</td>
    <td class="text-center">
        <a asp-page="/Accounts/Account" asp-route-id="@Model.Id" class="btn btn-sm btn-outline-primary">
            <i class="fa fa-eye me-1"></i> View
        </a>
    </td>
</tr>
